"use client"
import ProductPageContainer from "./Product/ProductPageContainer";

const ProductPage = ({ lang }: { lang: string }) => {
  return <ProductPageContainer lang={lang} />;
};

export default ProductPage;
